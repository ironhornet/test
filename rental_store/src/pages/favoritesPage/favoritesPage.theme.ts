import { makeStyles } from "@mui/styles";

export const useStylesFavoritesPage = makeStyles(() => ({
  pageWrapper: {
    display: "flex",
    flexWrap: "wrap",
    justifyContent: "start",
    rowGap: "35px",
    columnGap: "30px",
  },
}));
